# 验证记录

日期：2026-09-15。仅使用虚构测试文本，未读取或处理真实钱包私钥。

## 环境

- macOS 26.5.1，Apple Silicon。
- Google Chrome 152.0.7977.84。
- Node.js 22.23.1；浏览器测试通过本机 Playwright 驱动 Chrome。
- 使用本地 `file://`，浏览器上下文置为离线；应用运行无安装步骤。

## 核心测试：14 项通过

包括 API 可用性、Unicode 完整往返、前导 U+FEFF 保留、与 Node crypto 双向互操作、错误密钥拒绝、密文封装每个字节篡改拒绝、截断/尾随/超长文件拒绝、空文本及超长文本拒绝、1 MiB 文本往返、随机 nonce 产生不同密文、严格密钥格式、规范 Base64、导出数据区与固定模板保留。

Node crypto 使用独立的 AES-GCM 接口按公开字段解析和认证，不借用网页解密函数产生期望明文。

## 浏览器集成测试：通过

- 从本地直接打开工具；下载随机密钥后重新选择；错误的重新选择被拒绝。
- 下载自带解密功能的加密网页，检查导出物不含测试明文或原始密钥。
- 使用独立 Node crypto 解密浏览器导出物，原文一致。
- 拒绝只有密文片段、没有完整页面的残缺导出物。
- 完整保存校验成功；无新编辑时清除文本和密钥；导出后编辑的新文本被保留。
- 离线重新打开导出的 HTML；错误密钥和被修改的密文均不输出原文。
- 正确密钥通过后，原文默认隐藏；显示、失焦隐藏、立即清除工作正常。
- 带有 HTML 标签和事件属性的原文按普通文本呈现，不作为页面代码执行。
- 原工具可导入 HTML 数据并解密，不执行导入文件中的脚本。
- 使用模拟时钟触发真实的 5 分钟自动清除处理；输入被清除。
- 清除发生在密钥文件异步读取期间，读取结束后旧密钥不会恢复到页面状态。
- 390 像素窄屏、800 像素视口下 200% 字体放大均无横向溢出。
- 完整流程未观察到 HTTP/HTTPS 请求或 JavaScript 页面异常。

## 界面检查

检查了桌面、窄屏和解密后的截图，输入、密钥选择、下载、结果隐藏和安全说明均可阅读。截图仅为测试工件，不作为真实私钥示例交付。

## 代码审查

额外执行了独立代理代码审查，发现并修复了完整网页保存校验、保留后续编辑和前导 U+FEFF 三个问题；针对性复查没有新的可操作问题。此类代码审查和功能测试不等同于第三方专业安全审计或密码模块认证。

## 未验证范围

未在本次交付中实测 Windows、Linux、Safari、Firefox 或手机系统。未声称能够抵御已控制的电脑、被替换的 HTML 执行代码、恶意扩展、密钥窃取或屏幕录制。没有部署、联网账户或服务器依赖。
